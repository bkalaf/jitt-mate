export interface BacklineTypes {
    closed: null | string;
    open: string;
    u: string;
    v: string;
    bare: string;
    x: string;
    bow: string;
    strappy: string;
}
export const BacklineTypes = {
    closed: null,
    open: 'open-back',
    u: 'u-shape-back',
    v: 'v-shape-back',
    bare: 'bare-back',
    x: 'x-cross-back',
    bow: 'bow-back',
    strappy: 'strappy-back'
};
